<%
 # app/views/home_teacher/index.html.erb
 %>

<%#= params.inspect %>

<ul>
  <% @periods.each do |p| %>
    <li><%= "S#{p[:session]}P#{p[:period]}: #{p[:courses].map do |c| c[:title] end.join ' & '}" %></li>
    <ul>
      <% p[:courses].each do |c| %>
        <li><%= "#{c[:title]}" %></li>
          <table class='table'>
            <tbody>
              <% c[:students].each do |s| %>
                <tr>
                  <td><%= link_to s[:name], "/home_student/show/#{s[:id]}" %></td>
                  <td><%= last_seen(s[:id]) %></td>
                  <td><%=  %></td>
                  <td><%= assessment_overview(s[:id], c[:assessments]) %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
      <% end %>
    </ul>
  <% end %>
</ul>
